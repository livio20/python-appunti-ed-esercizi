help()/o help(x)

float(numeri in virgola mobile),int(interi), str(stringhe di caratteri Unicode) e bool(booleano).
int -> float -> complex. "operazioni" + aggiunta  -Sottrazione / Divisione  // Divisione intera  % Modulo (resto)  * Moltiplicazione  ** Esponente (potenza)

2Operatori Relazionali" == Vero, se è uguale  !=Vero, se non uguale a  < meno di  > più grande di  <= minore o uguale a  >= maggiore o uguale a
round( ) La funzione arrotonda il valore di input a un numero specificato di posizioni o all'intero più vicino.
abs( ) fornisce il valore assoluto di qualsiasi numero (inclusa la grandezza di un numero complesso).
  
stringhe in funzioni- mettendo il nome della funzione con. dopo la stringa upper(), lower(), captialize(rende la prima lettera della stringa solo maiuscola), title(seleziona maiuscole per la prima lettera di ogni parola.)e swapcase()
len()funzione fornisce la lunghezza di una stringa
  
partition(separator): spezza una stringa in tre parti in base a un separatoresplit(): divide la stringa in parole separate da spazi (opzionalmente accetta un separatore come argomento)join(): unisce il risultato di una divisione usando la stringa come separatore
  
  
Lists(lista) Gli elenchi vengono dichiarati semplicemente eguagliando una variabile a '[ ]' o elenco.
Slicing(Affettare)"tagliare" la lista.[ a : b ] dove a,b sono i valori dell'indiceaob non è definito, il valore dell'indice è considerato il primo valore per a se a non è definito e l'ultimo valore per b quando b non è definito.













